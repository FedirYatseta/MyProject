let initialState = {
    sale: [
        { id: 1, post: 'Чому варто вибрати нас?' },
        { id: 2, post: 'Скільки часу потрібно для впровадження системи?'}
    ]
}

const MainPageReducer = (state = initialState) => {
    return state
}

export default MainPageReducer;