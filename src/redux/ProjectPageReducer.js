let initialState = {
    projects: [
        { id: 1, attr1: 'zabbix' },
        { id: 2, attr1: 'ELC' }
    ]
}

const ProjectPageReducer = (state = initialState) => {
    return state
}

export default ProjectPageReducer;